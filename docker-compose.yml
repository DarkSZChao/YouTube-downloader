services:
  youtube-downloader:
    build: .
    container_name: youtube-downloader
    restart: unless-stopped
    ports:
      - "4658:8000"
    environment:
      ENV_COOKIES: ${ENV_COOKIES:-}
    volumes:
      - ./assets/downloads:/app/assets/downloads
      - ./config/config.yaml:/app/config/config.yaml
